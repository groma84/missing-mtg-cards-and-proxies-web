{
  "name": "missing-cards-web",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "test": "vitest"
  },
  "devDependencies": {
    "vite": "^2.7.2",
    "vitest": "^0.3.4"
  }
}
